import { style } from 'typestyle';

export const NestArray = style({});

export const Nest = style({});
export const Inline = style({});
export const Add = style({});
export const Edit = style({});
export const Remove = style({})